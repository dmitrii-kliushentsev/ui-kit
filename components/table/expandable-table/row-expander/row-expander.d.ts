interface Props {
    className?: string;
    expanded?: boolean;
    onClick: () => void;
}
export declare const RowExpander: ({ className, expanded, onClick, }: Props) => JSX.Element;
export {};
