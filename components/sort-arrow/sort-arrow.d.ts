interface Props {
    className?: string;
    order: 'ASC' | 'DESC' | null;
    active?: boolean;
}
export declare const SortArrow: ({ className, order, active }: Props) => JSX.Element;
export {};
